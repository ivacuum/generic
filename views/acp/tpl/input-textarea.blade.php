<textarea {{ $required ? 'required' : '' }} class="form-control textarea-autosized js-autosize-textarea {{ $errors->has($name) ? 'is-invalid' : '' }} {{ implode(' ', $classes) }}" name="{{ $name }}" rows="2" placeholder="{{ $placeholder }}" id="{{ $entity }}_{{ $name }}">{{ old($name, $model->{$name}) }}</textarea>
